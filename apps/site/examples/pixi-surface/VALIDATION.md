# Validation

Validated on 2026-09-21 with Edge 153.0.4234.48 and NVIDIA Turing WebGPU.

- npm test: **735 passed**, zero failures.
- npm run typecheck: passed across all workspaces.
- npm run build:pages: passed; 190 built HTML pages and deployment links verified.
- npm run docs:check: passed.
- tests/gpu/run.mjs: eight rendering/lifecycle cases passed, plus trusted pointer
  orbit, zoom, pause/reset, blur and pointer-cancel assertions.
- tests/gpu/production.mjs: five production cases passed, including the real
  Inspector, source reader, controls, identical paused pixels, SPA remounts,
  narrow layout and DPR 2.

The screen's encoded background read back as RGB **12, 20, 30**, exactly matching
Pixi's input. Front/back sphere fixtures verify shared reverse-Z occlusion.
Consecutive frames show updated sprite pixels. Disposing Pixi leaves the host
texture and device usable. Startup abort, visibility, device loss and repeated
dispose settle pending snapshot requests.

No GPU validation, uncaptured GPU, page or console errors were observed.
The standalone harness retains Pixi 8.21.0's known teardown BindGroup warnings
for destroyed texture sources/samplers; these are not suppressed. Production
also reports the existing Inspector wheel-sensitivity warning. The build reports
its existing large-chunk advisory.

Screenshots, snapshots and machine-readable reports are under
.test-dist/pixi-surface-gpu/ (regenerated by the GPU scripts; not committed).
The sprite matches its original MIT-licensed PixiJS v1.6.0 distribution byte for byte;
see THIRD_PARTY_NOTICES.md for the pinned provenance and retained license.
