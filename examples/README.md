# Repository examples

This directory contains private, application-like showcases that do not yet
belong to a single published package. They may be shared by website surfaces,
but they do not define a common example runtime contract or a recommended
package-level recipe structure.

An example should move into a package only after its host interface, teaching
value, and long-term maintenance expectations are clear.
